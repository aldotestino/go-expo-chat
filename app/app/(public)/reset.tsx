import React from "react";
import { View, Text, SafeAreaView } from "react-native";

const reset = () => {
  return (
    <SafeAreaView>
      <View>
        <Text>Reset</Text>
      </View>
    </SafeAreaView>
  );
};

export default reset;
